version: "3.8"
services:
  protein-visualizer:
    build: .
    image: protein-visualizer:latest
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
